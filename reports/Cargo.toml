[workspace]
members = [
  "cloud-stack-report",
  "cloud-health-full-report",
  "cloud-mail-full-report",
  "cloud-url-health-report",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
